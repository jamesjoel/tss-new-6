let price = 45000;
let type = 3;

if(type==1)
{
    let x = price * 5 / 100;
    let gst = price + x;
    console.log(gst);
}
if(type==2)
    {
        let x = price * 8 / 100;
        let gst = price + x;
        console.log(gst);
    }
    if(type==3)
        {
            let x = price * 18 / 100;
            let gst = price + x;
            console.log(gst);
}